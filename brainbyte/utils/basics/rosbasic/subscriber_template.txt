#!/usr/bin/env python3
"""
Template de Subscriber ROS
Use este arquivo como base para criar um subscriber em seu projeto ROS.
Substitua {topic_name} pelo nome do seu tópico
Substitua {message_type} pelo tipo de mensagem (ex: std_msgs.String)
"""

import rospy
from std_msgs.msg import String, Int32, Float32
# TODO: Importe o tipo de mensagem correto

class {topic_name}Subscriber:
    def __init__(self):
        """Inicializa o subscriber."""
        self.topic_name = '{topic_name}'
        self.last_message = None
        # TODO: Defina o tipo de mensagem correto
        rospy.Subscriber(self.topic_name, String, self.callback)
        rospy.init_node('subscriber_node')
    
    def callback(self, data):
        """Callback chamado quando uma mensagem é recebida."""
        self.last_message = data
        rospy.loginfo(f"Recebido: {data.data}")
        # TODO: Processe a mensagem conforme necessário
    
    def run(self):
        """Mantém o subscriber ativo."""
        rospy.loginfo(f"Subscriber ativo no tópico: {self.topic_name}")
        rospy.spin()

def app():
    """Função principal."""
    try:
        sub = {topic_name}Subscriber()
        sub.run()
    except rospy.ROSInterruptException:
        pass

if __name__ == '__main__':
    app()
